# Docker runtime dependency layer. Keep direct pins aligned with pyproject.toml.
# Installed before application source is copied so normal code rebuilds reuse this layer.
email-validator==2.2.0
hatchling==1.27.0
httpx==0.28.1
playwright==1.52.0
pydantic==2.11.5
pydantic-settings==2.9.1
typer==0.16.0
